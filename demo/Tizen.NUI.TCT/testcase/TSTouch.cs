using NUnit.Framework;
using NUnit.Framework.TUnit;
using System;
using Tizen.NUI;
using Tizen.NUI.BaseComponents;
using Tizen.NUI.Test;

namespace Tizen.NUI.Tests
{
    [TestFixture]
    [Description("Tizen.NUI.Touch Tests")]
    public class TouchTests
    {
        private string TAG = "NUI";

        [SetUp]
        public void Init()
        {
            Tizen.Log.Info(TAG, "Init() is called!");
            App.MainTitleChangeText("TouchTests");
            App.MainTitleChangeBackgroundColor(null);
        }

        [TearDown]
        public void Destroy()
        {
            Tizen.Log.Info(TAG, "Destroy() is called!");
        }

        [Test]
        [Category("P1")]
        [Description("Create a Touch object. Check whether Touch is successfully created or not.")]
        [Property("SPEC", "Tizen.NUI.Touch.Touch C")]
        [Property("SPEC_URL", "-")]
        [Property("CRITERIA", "CONSTR")]
        [Property("AUTHOR", "Wu Youxia, youxia.wu@partner.samsung.com")]
        public void Touch_INIT()
        {
            /* TEST CODE */
            var touch = new Touch();
            Assert.IsNotNull(touch, "Can't create success object Touch");
            Assert.IsInstanceOf<Touch>(touch, "Should be an instance of Touch type.");
        }
    }
}
